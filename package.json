{
  "name": "pixi-rive-workspace",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "cd packages/@schmooky/pixi-rive && npm i && npm run build",
    "dev": "cd examples/demo && npm i && npm run dev",
    "preview": "cd examples/demo && npm run preview"
  },
  "dependencies": {
    "@rive-app/canvas": "^2.31.2"
  }
}
